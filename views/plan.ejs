<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/style1.css" type="text/css">
    <title>Plan</title>
</head>

<header>
        <%- include ('partials/header') %>
</header>

<body>
    <h1 class="fitness-name">fitness</h1>
    <div class="page-grid">
        <div>
            <p>rank: 14</p>
            <p>workouts completed: 124</p>
            <canvas id="myChart"></canvas>
        </div>
        <div>
            test2
        </div>
    </div>
    <!-- <div class="plan-container">
        <% workoutNames.forEach(name => { %>
            <% if (name.name != null) { %>
            <table>
                <thead>
                    <th colspan="1">
                        <%= name.name %>
                        <form style="float: right; margin-left: -50%;" action="/plan/edit" method="POST">
                            <button type="submit" name="editday" value="<%= name.day %>">&#9998</button> 
                        </form>
                    </th>
                </thead>
                <tbody>
                    <% workouts.forEach(workout => { %>
                        <% if (workout.name == name.name && workout.name != null) { %>
                            <tr>
                                <td colspan="3">
                                    <%= workout.exercise_name %>
                                </td>
                            </tr>
                        <% } %>
                    <% }) %>
                </tbody>
            </table>
            <% } %>
        <% }) %>
        <form action="/plan/edit" method="POST">
            <button class="add-workout-btn">+ADD WORKOUT</button>
        </form>
    </div> -->
</body>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <script>
    const ctx = document.getElementById('myChart');

    new Chart(ctx, {
        type: 'doughnut',
        data: {
        labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
        datasets: [{
            label: '# of Votes',
            data: [12, 19, 3, 5, 2, 3],
            borderWidth: 3,
        }]
        },
        options: {
            layout: {
                padding: {
                    left: 80,
                    right: 80
                }
            }
        }
    });
    </script>
</html>